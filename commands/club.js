const app = require('../main');

module.exports = {
    name: 'club',
    description: 'Créer un nouveau raid à distance',
    args: true,
    usage: '',
    async execute (message, args) {

    }
};
